# 도커/배포 전용 프로파일 — 값은 환경변수에서 주입
lostark:
  api:
    key: ${LOSTARK_API_KEY}
    base-url: https://developer-lostark.game.onstove.com

server:
  port: 8080

logging:
  level:
    "[org.springframework.web.servlet.resource]": ERROR
